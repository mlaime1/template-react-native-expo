import React from 'react';
import { View, Text } from 'react-native';

interface CardProps {}

const Card: React.FC<CardProps> = () => {
  return (
    <View>
      <Text>Card</Text>
    </View>
  );
};

export default Card;